#include <stdio.h>

int main(int argc, char **argv)
{

    return 0;
}

// // if l rotate a tree then r rotate a tree we get the original tree we started with
// ll rotate r with imbalanced node
// rr rotate l with imbalanced node
// lr rotate left child of first imbalanced node and right roate with imbalanced node
// rl rotate right child of first imbalanced node and left roate with imbalanced node
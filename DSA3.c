#include <stdio.h>

int main(int argc, char *argv)
{

    return 0;
}

// Big O upper bound

// 0 <= f(n) <= c*g(n) for all n >= n0

// Big Omega lower bound

// 0 <= c*g(n) <= f(n) for all n >= n0

// Big Theta upper bound and lower bound

// 0 <= c1*g(n) <= f(n) <= c2*g(n) for all n >= n0 n
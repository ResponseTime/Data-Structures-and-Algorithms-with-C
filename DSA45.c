#include <stdio.h>

int main(int argc, char **argv)
{

    return 0;
}
// avl tree is height balanced tree
// balaned factor = height of right subtree - height of left subtree
// if all nodes have balanced factor in [1,0,-1] it is said to be an avl tree
// if we insert an element in an avl tree the balance factor of nodes might get unbalanced to combat that we use rotations'
